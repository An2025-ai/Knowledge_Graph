# Local embedding/reranker models (OPTIMIZATION_TECH_PLAN.md §10.7)
# USED: bge-m3 + bge-reranker-v2-m3 via sentence-transformers (local provider).
# Download uses the HF_ENDPOINT mirror when set (hf-mirror.com) for networks
# that cannot reach huggingface.co directly.
#   pip install -r runtime/requirements-local-models.txt
#   set HF_ENDPOINT=https://hf-mirror.com   # if huggingface.co unreachable
sentence-transformers>=3.0
torch
transformers
accelerate

# PaddleNLP small-model NER (OPTIMIZATION_TECH_PLAN.md §10.3) — OPTIONAL.
# Adds the PaddleNLP NER layer in candidate_pre_extraction (organization /
# product / capability / certification candidates). Heavy; PaddlePaddle may not
# build on every Python/Windows combo. NOT installed -> the NER layer degrades to
# empty and the L2/L3 pipelines run unchanged. Install only if NER is needed.
#
# VERSION PIN: the UIE Taskflow needs the static-graph export, which FAILS under
# paddlepaddle 3.x (missing static/inference.pdmodel). Verified working pair on
# Windows/Py3.12: paddlepaddle==2.6.2 + paddlenlp==2.6.1.
#   pip install -r runtime/requirements-local-models.txt
paddlenlp==2.6.1
paddlepaddle==2.6.2