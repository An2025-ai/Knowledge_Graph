# Dependencies for the local-first desktop/web application.
# Intentionally excludes PostgreSQL, Neo4j, torch, PaddlePaddle and local models.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
keyring>=25,<26
PyYAML>=6.0,<7
jsonschema>=4.20,<5

# Optional document formats for the next import adapters.
pymupdf>=1.24
python-docx>=1.1
openpyxl>=3.1

# Build-only dependency for the Python sidecar.
pyinstaller>=6.10,<7
